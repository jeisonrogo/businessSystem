# Database Configuration
DATABASE_URL=postgresql+psycopg://business_admin:pc5dTLr38WkwkGlhq4F5@business-system-dev-db.cqrk2ecg4sic.us-east-1.rds.amazonaws.com:5432/business_system_dev


# JWT Configuration
JWT_SECRET_KEY=6007d0d0133964b085e58bcc6cb7e32a96b11e4eaee81f2af98d54be0729700c-very-long-and-secure
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application Configuration
BASE_URL=http://localhost:8000

# Storage Configuration
# Options: "local" or "s3"
STORAGE_TYPE=local

# Local Storage Settings (when STORAGE_TYPE=local)
UPLOAD_DIR=uploads

# AWS S3 Settings (when STORAGE_TYPE=s3)
# Required when STORAGE_TYPE=s3
AWS_S3_BUCKET_NAME=your-bucket-name
AWS_REGION=us-east-1

# Optional: AWS credentials (if not using IAM roles)
# Leave empty to use IAM role authentication (recommended for production)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# CORS Configuration
CORS_ORIGINS=[http://localhost:3000,http://localhost:5173]
